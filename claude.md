Stack = Netlify + Git + Github + hébergement des assets sur bunny (clé API dans .env)
Tu dois respecter la charte graphique dans "Ressources"

Objectif : utiliser le prototype comme modèle est créé une version FR / NL avec les ressources existantes 

tu peux créer un git en local, on fera ensuite le github en ligne pour déployer sur netlify 